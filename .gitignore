# The built Go binary (the name produced by `make build`)
/tugbot

# Vendored dependencies
/vendor/

# Local environment (real tokens / credentials)
.env

# Per-developer MCP client registration (points at the local bot instance)
.mcp.json

# Test artifacts
/test-artifacts/
*.test
coverage.out
