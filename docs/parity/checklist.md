# tugbot Go port — behavior parity checklist

## interaction dispatch (main wiring)
- [ ] `InteractionCreate` guard: `Type == 2` (application command) + `Data != nil`; **discordgo v0.29.0 stores `Interaction.Data` as a VALUE** (`UnmarshalJSON` does `i.Data = v`) — the assertion must be the value form `i.Data.(discordgo.ApplicationCommandInteractionData)`; the pointer form `(*...)` ALWAYS fails and silently drops every command ("The application did not respond", zero bot logs — proven live at cutover)
- [ ] dispatch by `Data.Name` in the pinned Rust order; one goroutine per interaction (handler arms do REST)
- [ ] `command received` INFO log per application command (name + guild) — a Go-side observability addition; Rust mod.rs was SILENT at `interaction_create`, do not revert it as a parity deviation

## teh
- [ ] gated on the `teh` feature flag via silent `IsEnabled` (Rust `Features::is_enabled`, false on DB error) (src: teh.rs:12)
- [ ] trigger = message content lowercased contains the substring "teh" (no regex, no word boundary) (src: teh.rs:12)
- [ ] on trigger: reacts "🇹", then "🇪", then "🇭" — three SEQUENTIAL reactions, applied in that exact order (src: teh.rs:14,18,22)
- [ ] each reaction failure is logged separately with the per-emoji message ("Error reacting with emoji T" / ... E / ... H) and the handler continues (src: teh.rs:15,19,23)
- [ ] message-gate wiring: invoked on every message create, first in `mod.rs` dispatch order (src: mod.rs:106)
- [ ] no guild/channel guards, no replies, no edits — reactions only (entire file: teh.rs)

## twitter
- [ ] gated on the `twitter` feature flag via silent `IsEnabled` (src: twitter.rs:12)
- [ ] trigger regex `https://(twitter\.com|x\.com)/.+/status/\d+` — status links from EITHER twitter.com or x.com, status id must be digits (src: twitter.rs:30)
- [ ] on match: rewrites the matched URL by replacing the matched domain group with `girlcockx.com`, then posts the rewritten URL — the POSTED message is only the matched URL substring, not the original content (src: twitter.rs:17,25,43-44)
- [ ] BEFORE posting: edits the ORIGINAL message to set the suppress-embeds flag (the official Serenity `suppress_embeds(true)` edit, an EDIT — not a content change) (src: twitter.rs:17-18)
- [ ] the new rewritten-URL message is a NEW message in the channel (`channel_id.say` → `ChannelMessageSend`-equivalent), NOT an in-place content edit (src: twitter.rs:25)
- [ ] logging parity: "Error supressing embeds" on edit failure, "Suppressed Embed" unconditionally after the edit, "Error Editing Message to Tweet" on say failure, "Posted Tweet" unconditionally after (src: twitter.rs:21,24,26,29)
- [ ] no guild/channel guards (entire file: twitter.rs); wired on every message create (src: mod.rs:107)
- [ ] manual parity test anchors (ported 1:1): twitter.com and x.com status URLs → `https://girlcockx.com/...`; non-status / non-twitter URLs → no match (src: twitter.rs:49-82)

## bsky
- [ ] gated on the `bsky` feature flag via silent `IsEnabled` (src: bsky.rs:12)
- [ ] trigger regex `https://(bsky\.app)/.+` — anything after bsky.app/ (profile, post, query params) (src: bsky.rs:29)
- [ ] on match: rewrites by replacing the matched domain group with `bsyy.app`, posts the matched URL substring as a NEW message (src: bsky.rs:16,25,43-44)
- [ ] BEFORE posting: edits the ORIGINAL message to set the suppress-embeds flag (src: bsky.rs:17-18)
- [ ] `https://bsky.app/` with nothing after the slash does NOT match (regex requires `/.+`) (src: bsky.rs:29,82); query params DO match (src: bsky.rs:88-91)
- [ ] logging parity: "Error supressing embeds" / "Suppressed Embed" / "Error Editing Message to Tweet" / "Posted Tweet" — same order as twitter (src: bsky.rs:21,24,26,29)
- [ ] no guild/channel guards (entire file: bsky.rs); wired on every message create (src: mod.rs:109)

## instagram
- [ ] gated on the `instagram` feature flag via silent `IsEnabled` (src: instagram.rs:12)
- [ ] trigger regex `https://(www\.)?(instagram\.com)/.+` — optional `www.` prefix (group 1, left untouched), domain in group 2 (src: instagram.rs:28)
- [ ] on match: rewrites by replacing only the domain group (group 2) with `kkinstagram.com`, preserving a leading `www.` when present, and posts the matched URL substring as a NEW message (src: instagram.rs:16,25,43-44)
- [ ] BEFORE posting: edits the ORIGINAL message to set the suppress-embeds flag (src: instagram.rs:17-18)
- [ ] parity test anchors (ported 1:1): `www.instagram.com` URL → `https://www.kkinstagram.com/...`; `instagram.com` without www → `https://kkinstagram.com/...`; empty / non-instagram → no match (src: instagram.rs:48-99)
- [ ] logging parity: "Error supressing embeds" / "Suppressed Embed" / "Error posting Instagram message" / "Posted Instagram" (src: instagram.rs:21,24,26,29)
- [ ] no guild/channel guards (entire file: instagram.rs); wired on every message create (src: mod.rs:110)

## feat
- [ ] WIRING (verified in mod.rs): NOT a message handler — slash command only; `ready()` registers `Feat::setup_command()` among the per-guild commands (src: mod.rs:257,294) and `interaction_create` dispatches command name "feature" to `Feat::setup_interaction` (src: mod.rs:226)
- [ ] slash registration shape: name `feature`, description `Toggle Feature`, single OPTION (not required) string option named `name` described "The feature to enable" (src: feat.rs:13-22)
- [ ] with `name` given, non-string option value → ephemeral error response "Please provide a valid feature name" (src: feat.rs:38-40)
- [ ] with `name` given: `Features::all` failure → ephemeral error response with the DB error text; on success, the feature list is matched BY NAME (src: feat.rs:34-36)
- [ ] no `name` option: `Features::all` failure → error response; success → the list response (src: feat.rs:42-44)
- [ ] toggle semantics: matched feature is flipped (`UPDATE features SET enabled = !enabled` via `Features::update` with `!feat.enabled`), a DB update failure → ephemeral "Failed to update feature: <err>" response, a successful toggle is followed by a FRESH `Features::all` list (src: feat.rs:49-62)
- [ ] no match in the list → exact response text "Couldn't match feature" (src: feat.rs:65-69)
- [ ] list format: header "Here's all the features" + one "\nName: `<name>` Enabled: `<true/false>`" line per feature, in DB order (src: feat.rs:74-83)
- [ ] ALL Feat responses are ephemeral (the Rust `HandlerResponse` literals each set `ephemeral: true`) and none sets components or defers (src: feat.rs:83-97)

## prefixhandler
- [x] wired in ready() as a prefix-command handler (not a message gateway); every message in a configured guild server is scanned for `<prefix> <subcommand>` (src: prefix.rs 15-29, Type::Command dispatch in mod.rs 193-210)
- [x] feature gate is the PROPAGATING CheckEnabled flavor (unlike the silent IsEnabled in ai_slop): DB error text is passed straight through to the user (src: prefix.rs 41-43)
- [x] unknown subcommand -> exact "Unknown command" (src: prefix.rs 88-90)
- [x] `remove-from-ban` branch: "Moderator" role guard (ephemeral guard text) -> member fetch (permVerify text on failure) -> role-required text -> add_to_gulag with ZERO duration -> exact "called remove-from-ban for {username}!" on success, "Failed to remove {username} from the GULAG" / "Error: Could not find ..." on failure (src: prefix.rs 92-131)
- [x] `send-to-gulag` branch: feature check (silent path -> disabled text) -> mod role guard -> member fetch -> role check -> check already-in-gulag ("{username} is already in the GULAG") -> get_or_create poll usage count -> GulagDurationForOffense(duration) with int32 clamp -> add_to_gulag -> increment usage ("failed to record" on failure) -> the-gulag notify -> exact final "Sent {username} to the GULAG for {duration}! (next offense will be {next})" with NextTotalSecondsForCount (src: prefix.rs 133-171)
- [x] all byte-string texts pinned in prefixhandler_test.go (combinatorial slice + exact matches)

## gokupoll
- [x] wired in ready() as a message-update handler (gateway dispatch in mod.rs 89-92); skip: no change / no poll / no results / not finalized (src: goku_poll.rs 18-40)
- [x] winner detection (pure): results.AnswerCounts max-count (tie -> first, Rust max_by_key) -> matching answer by ID -> Media.Text; no votes / no text / no media -> skip (src: goku_poll.rs 42-70); unit-tested via vendored discordgo Poll fixtures
- [x] poll creator bot check: Some(true) -> skip, Some(false) -> continue, None -> skip (src: goku_poll.rs 72-80)
- [x] server lookup by guild ("failed to find server" / "not configured" texts); usage count fetched BEFORE the increment (TOCTOU avoidance, per Rust comment); lapsed/negative count -> 30-day cap + error log (src: goku_poll.rs 85-105)
- [x] the-gulag channel lookup: lookup error text / missing text handled separately (src: goku_poll.rs 107-120)
- [x] add_to_gulag with caller-side int32 clamp (DurationToGulagLength) then increment_gulag_poll_usage; increment failure -> "failed to record poll" text + no notification (src: goku_poll.rs 122-145)
- [x] the-gulag notification: `{creator.mention} has been sent to the GULAG for {duration} for starting a poll: {message link}`; final response `Sent {username} to the GULAG for {duration} for starting a poll! (next offense will be {next})` via NextTotalSecondsForCount (30-day clamp) - all byte-for-byte (src: goku_poll.rs 147-190)

## aislop
- [x] registration is a MESSAGE CONTEXT-MENU command: kind = ApplicationCommandTypeMessage, name "AI Slop", EMPTY description, no options (verified against mod.rs:291 dispatch; the "Add Gulag Vote" registration belongs to Task 6 and is NOT registered here) (src: ai_slop.rs 22-26)
- [x] silent IsEnabled gate (no DB-error response path - differs from the prefix handler's CheckEnabled) (src: ai_slop.rs 28-34)
- [x] guard order: non-guild -> guildOnly text; member fetch (permVerify text); role gate via canonical core MemberHasAnyRole (fetch guild roles once + scan member role IDs; "Highly Regarded" / "admin"); first resolved message (Rust HashMap.values().next() iteration is arbitrary - Go map iteration is likewise arbitrary, pinned here); self-slop; bot-target branch Some(true) / Some(false) / None (src: ai_slop.rs 36-96)
- [x] server lookup: "not configured ... gulag role" text; atomic upsert increment_ai_slop_usage returning the NEW count (src: ai_slop.rs 98-120)
- [x] offense duration: new_count.saturating_sub(1) -> exponential seconds lookup with the int32 try_into arm preserved ("Usage count too high" text) - delegated to the canonical core; add_to_gulag with integer clamp via DurationToGulagLength ("Failed to send to gulag: {err}" text) (src: ai_slop.rs 98-131)
- [x] the-gulag notification sent ONLY when the channel exists; 404 lookups and send failures swallowed; message = creator mention + " has been sent to the gulag for {duration} for posting AI slop: {message link}\nThis is offense #{n}" byte-for-byte (src: ai_slop.rs 132-162)
- [x] final response: "Sent {username} to the gulag for {duration} for posting AI slop!\nThis is their offense #{n} (next offense will be {next})" with NextTotalSecondsForCount (30-day clamp) (src: ai_slop.rs 179-197)

## mention
- [ ] 1. feature-flag gate on DB key `is_this_real` via silent `IsEnabled` (false on any error); disabled → handler returns before logging "Handler called by" (src: mention.rs:78-80, Go `mention.go` step 1)
- [ ] 2. bot-mention check via the API-provided mention list only (discordgo v0.29.0 `Message.Mentions`, populated by the gateway) — NEVER a string scrape of the raw content; `get_current_user` failure → log "Failed to get current user" + return (src: mention.rs:83-96; Go `mentionsBot` + `botUserID`, State fast-path + REST `GET /users/@me` fallback mirroring Rust's unconditional HTTP call)
- [ ] 3. guild-ID check: messages without a guild ID return silently (needed for the special-user path in step 5) (src: mention.rs:98-99; Go `m.GuildID == ""` guard, v0.29.0 has no Option-typed guild id)
- [ ] 4. CHANNEL RESTRICTION: only `#ask-tugbot` (`ASK_TUGBOT_CHANNEL_ID = 1515343076401479790`, pinned constant `AskTugbotChannelID`) — mentions anywhere else in the guild return silently (test-proven: wrong channel → 0 pi asks; right channel → 1 pi ask; empty-guild DM → 0 pi asks) (src: mention.rs:101-102)
- [ ] 5. CONFIG + SLOW-USER AUTO-GULAG fires BEFORE question extraction (Rust lines 113-125): only when the author is in `SlowUserIDs` AND the `slow_user_auto_gulag` flag is enabled (DB key literal `slow_user_auto_gulag` — Rust `SLOW_USER_AUTO_GULAG_FEATURE`, default OFF via migration 2026-06-13-200000) → 5-minute (300s) gulag via the canonical Task-4 core (`FindChannel("the-gulag")` + `AddToGulag` with `GulagLength = 300 = GULAG_DURATION_SECS`), porting the exact channel message text "{} wanted to know if something was real... now they're in the gulag for 5m. Irony." (mention carries the `<@ID>`); the port preserves Rust's eprintln texts ("No server config for guild {} (or DB unavailable)", "gulag role ID {} overflows u64", "No gulag channel found", "Error looking up gulag channel: {}", "Failed to gulag slow user: {}", "Failed to send gulag message: {}"). POSITION: flag-on + empty question → gulag (does NOT fall through to the step-6 empty-question path) (src: mention.rs:113-125, 412-473)
- [ ] 6. QUESTION EXTRACTION is Rust's exact algorithm (lines 126-153): `split_whitespace`, per-token `trim_start_matches("<@")` → `trim_start_matches('!')` → `trim_end_matches('>')`, drop tokens whose **STIPPED form** equals the bot ID (the KEPT token is the ORIGINAL one — test-pinned via `<@456>`/`<@999>` cases that would break with a naive kept-stripped port); empty → send a **PLAIN (non-reply)** "You mentioned me but didn't ask anything — what's up?" and return (test-proven: one `ChannelMessageSend`, zero replies, zero pi asks) (src: mention.rs:126-153)
- [ ] 7. REFERENCED-MESSAGE FETCH only when the current message is a reply: Rust's `msg.message_reference.as_ref().and_then(|r| r.message_id)` → Go `m.MessageReference.MessageID` (v0.29.0 shape); fetch failure → log "Failed to fetch referenced message" and treat as `None` — the flow CONTINUES to the pi ask with the absent-reference template (test-proven: fetch-error → 1 pi ask, absent template) (src: mention.rs:156-166)
- [ ] 8. COOLDOWN CHECK: 5m (`COOLDOWN_SECS = 300`) normal / 2h (`SLOW_COOLDOWN_SECS = 7200`) for slow users; `CooldownExemptUserIDs` bypasses the check entirely; no-usage-row → no gate (first use passes). Elapsed is integer seconds (floor, clamped at 0 under clock skew — Rust's `unwrap_or_default()`). On block: REPLY to the original message (Rust `reference_message((channel_id, msg.id))`), MAPPING per Rust line 196 (test-pinned on BOTH variants, not just the literals): `slow_user_ids.contains(user)` → `"Easy there, {<@ID>} — give it a rest for {t}"`; else → `"I'm still waking up — try again in {t}"`; `{t}` from `formatRemaining` (the MENTION formatter: `>=3600s` → `"Xh"` or `"Xh Ym"` non-zero minutes only; `>=60s` → `"Xm"`; else `"Xs"` — table-pinned at 0s/1s/45s/59s/60s/59m/1h/1h 1m/2h/2h 1m; distinct from the gulag `FormatDuration`, both keep their Rust names). Blocked users never reach reactions/pi; no cooldown write on the block path. (src: mention.rs:169-215)
- [ ] 9. REACT order: `:eyes:` (👀) to acknowledge FIRST, then `:thinking:` (🤔) while processing — exact order at this position (BEFORE image download / the ask); per-reaction success/error log parity ("Reacted with :eyes:" / "Reacted with 🤔" / "Failed to react") (src: mention.rs:218-229)
- [ ] 10. IMAGE DOWNLOAD from the referenced message: one shared HTTP client with an explicit 10s timeout (Go `http.Client{Timeout: 10 * time.Second}`, mirroring Rust's `reqwest` 10s); MIME sourcing BY SOURCE (test-pinned via the plan table + a live httptest round-trip): attachments use the attachment's own `content_type` (empty → `"application/octet-stream"`, then filtered on the `image/` prefix, then `is_safe_url`); embed/thumbnail URLs use the extension→MIME map (query-string + fragment stripped first; png/gif/webp else jpeg; NO lowercasing — Rust `Path` semantics; trailing-dot = no extension) and are DEDUPED against the attachment URLs first (against ALL attachment URLs — Rust's dedupe is unfiltered); `is_safe_url` ported 1:1 (`http://`/`https://` prefix only) with the Rust skip-log texts ("Skipping unsafe URL: {}" / "Skipping unsafe embed URL: {}"); per-URL download/read failures are logged and skipped (flow continues with the rest) (src: mention.rs:232-294)
- [ ] 11. PI RPC: `app.Pi == nil` → log `pi RPC not available` and RETURN SILENTLY, before the error reply (🤔 stays in place) — test-proven: the nil-pi flow logs the line, sends zero replies/messages, performs zero cooldown writes (src: mention.rs:297-304)
- [ ] 12. PROMPT: the two branches are referenced-message PRESENT vs ABSENT — the slow/normal distinction NEVER branches the prompt. Pinned byte-identical: absent → `"{author} asked: "{q}\""`; present → `"{author} replied to: "{ctx}\"" and asked: "{q}\""` with the 4-way content×image context matrix (content+images → `"{content} [also shared an image]"`; images-only → `"[shared an image ({n})]"`; content-only → raw content; neither → `"[replied to an image]"`; the image count is the number of downloaded images) (src: mention.rs:306-320; all 5 template cases table-pinned)
- [ ] 13. ASK: `AskWithImages(ctx, prompt, images)` on the pi backend (the 300s ask timeout lives in the pi package, not here) (src: mention.rs:322-323)
- [ ] 14. TRIM-THEN-CHECK-EMPTY: whitespace-only responses count as empty → REMOVE 🤔, skip the post AND skip the cooldown write-back (test-proven with the fake PiBackend: zero sends, zero writes, the log line fires; verified against the non-empty control that posts AND writes); on an ask ERROR: remove 🤔 and REPLY (to the original message) "I'm having trouble thinking right now, try again later" (exact, test-pinned) with send-failure parity log; on success: remove 🤔, log "Posting response...", POST as a REPLY to the original message (Rust `reference_message((channel_id, msg.id))` → `ChannelMessageSendReply`), log "Response posted" / "Failed to post response: {}" (src: mention.rs:322-390)
- [ ] 15. COOLDOWN WRITE-BACK only when `posted && !exempt` (Rust lines 398-410): `get_or_create_is_this_real_usage` (select by `(user_id, guild_id)`; insert with `last_used_at = now()` on missing) then `update_is_this_real_usage` (`last_used_at = now()`) — the get_or_create failure arm is silent (Rust `if let Ok`); an UPDATE failure logs "Failed to update cooldown: {}" (test-proven: a successful post writes exactly once; exempt users never write; failed posts write zero) (src: mention.rs:398-410; Go `poolStore.usageReset` on raw SQL identical to the Task-1 sqlc files)

## gulag
- [x] GulagDurationForOffense: 1800 * 2^(count-1) with saturating semantics; the count >= 32 branch caps at the int64 max (2^63-1) without panic - test table covers counts 0..>=32
- [x] the caller-side int32 clamp is preserved as a SEPARATE layer: DurationToGulagLength (int64 seconds -> int32, overflow -> 2_592_000 = 30-day fallback) - mirrors Rust's two-stage try_into conversion
- [x] DB write path: AddToGulag / ComputeNewGulagTime on the existing row perform checked int32 addition overflow + non-negative validation with the exact Rust error text ("gulag length overflow") - tests cover overflow / zero / negative
- [x] FormatDuration matches Rust format_duration exactly: hours > 0 -> "Xh Ym" (zero minutes included); else minutes > 0 -> "Xm"; else "Xs" - test table pinned (90s -> "1m", 900 -> "15m", 3600 -> "1h 0m", 9900 -> "2h 45m", 2592000 -> "720h 0m")
- [x] MemberHasAnyRole: fetches guild roles ONCE + scans member role IDs, matching role NAMES exactly (no normalization/case folding) - shared by aislop, prefixhandler, and the Task 6 core; pure helper, unit-tested
- [x] the core accessors (FindChannel, SelectServerByGuildID, IsTugbot, usage counters) are implemented on top of app.Pool via the tasks 1-4 DB helper functions - Tasks 5/6 reuse this same core; no handler re-declares any helper
- [ ] Dead-code notes (verified against mod.rs ready() + interaction_create dispatch): `gulag-vote` (GulagVoteHandler — slash incl. process_gulag_votes / do_followup / spam detection / jury duty, gulag_vote.rs) is DEFINED BUT NOT REGISTERED in ready() nor in the dispatch table — no registration and no dispatch arm ported; `message_vote_remove` in src/db/message_vote.rs is likewise unreachable (no call sites) — not ported. `Add Gulag Vote` IS registered + dispatched, and is the only vote-gating path (src: gulag/mod.rs ready() + interaction_create; gulag_vote.rs entire)
- [ ] Rejoin flow (guild_member_addition in src/handlers/mod.rs:144-213, ported as the gulag package JoinRejoin; Task 7 wires OnGuildMemberAdd): canonical core IsUserInGulag lookup by user id; five separate checked-conversion error texts each log + RETURN (Guild ID / User ID / Gulag role ID / Gulag length / Channel ID conversion error) - in Go the int64 domain makes the DB->Discord conversions successful by construction, and the length uint32 clamp mirrors the Rust i32 -> u32 arm; re-AddToGulag with the STORED length + role (the existing branch in the core doubles the length and extends release_at) - an AddToGulag failure only logs (Failed to re-add user to gulag on rejoin) and the flow still sends the escape message; escape = post "You can't escape so easily <@!ID>" to the STORED channel (channel fetched by raw channel id - the the-gulag-name lookup is NOT used here); channel-fetch failure is swallowed (Rust if let Ok(channel)); portal is sent via raw-channel-id send (Rust channel.id().send_message) (src: mod.rs:144-213)
- [ ] Reaction vote (gulag_reaction.rs, 179 lines; Rust ready() wires BOTH reaction_add and reaction_remove to the ONE handler; the handler examines the live message so a removed reaction re-counts): trigger emoji's serialization (custom <name:ID> / unicode itself - the Emoji.MessageFormat equivalent) must contain the substring "gulag"; silent IsEnabled "gulag" gate; missing guild id returns silently; message fetch failure logs Failed to fetch message {} and returns; among the message's reactions the FIRST one whose emoji serialization contains ":gulag" is tallied (per-reaction log with count: {}); then manual get_reaction_users pagination: PAGE_SIZE 100 (Discord per-call cap), MAX_PAGES 50, IMMEDIATE paging with NO inter-page delay (a per-page sleep would stall up to 50s per reaction event), cursor = last page user id (empty cursor breaks), short page (< 100) ends, hitting the 50-page cap logs the WARNING: hit MAX_PAGES warning; ALL bots are filtered (Rust !u.bot - every bot, not just this bot); NO FOR UPDATE here (sync_from_discord / message_vote_create_or_update are plain non-txn SQL - the SKIP LOCKED locking belongs to the loop queries below); sync overwrites current_vote_tally = voter count + voters for an existing row, or creates a fresh row (status created, total 0) for a missing one; logs: Synced votes for message {}: {} votes on success, NO MATCH - trigger={} diagnostic (listing all reaction strings) when no :gulag reaction, Failed to fetch reaction users / Error syncing votes on failure; test-pinned: a synthetic 150-voter fixture pages 100+50 and counts exactly 150, an all-full 50-page run caps at 5000, bots in the pages are excluded (src: gulag_reaction.rs:7-59, 62-170)
- [ ] Vote state machine (src/db/message_vote.rs + run_gulag_vote_check in gulag/mod.rs - NOT gulag_vote.rs): job_status values byte-identical created / running / done / failure (the sqlc JobStatus enum); ONE vote per user per message - message_vote_create_or_update appends to voters bigint[] with the idempotency error text You have already Voted; message_votes.user_id (the VOTED person) is the message AUTHOR while the voter (the invoker) lands in the borrowers array; current_vote_tally increments by 1 per added voter; there is NO vote-window concept anywhere in Rust - do not invent a 300s window; voters bigint[] is the whole-tally source of truth (sync overwrites it from Discord, done clears it) (src: message_vote.rs:32-186)
- [ ] Add Gulag Vote (message context menu, kind = ApplicationCommandTypeMessage, name byte-identical, EMPTY description, no options - one of the 4 registered shapes): gates in this order: silent IsEnabled text Gulag feature is currently disabled. (WITH the period - note the /gulag slash gate text has NO period); target_id == empty -> No target message found.; resolved.messages[target_id] missing -> Could not resolve target message.; missing guild -> This command can only be used in a server.; message_vote_create_or_update(message id, guild id, interaction channel id, the message's AUTHOR id, the invoker id) - the gulaged user is that message's author; success: A gulag vote has been added to <msg link> / removed from <msg link> + \nThere are now N unique votes total (N = current_vote_tally; the REMOVED arm text exists like Rust but is unreachable - nothing calls the remove path); DB errors surface as the raw error text, all ephemeral (src: gulag_message_command.rs:16-104)
- [ ] Vote loop (RunVoteCheck, port of run_gulag_vote_check): 1s rhythm; FIRST the >= 30s stale Running -> Created reset (shared LAST_RESET seconds epoch, Rust static AtomicU64; the reset UPDATE is plain and its failure is swallowed like Rust .ok() - test-pinned: a fresh timestamp suppresses, a 31s-stale one resets a running row to created); THEN the pending query: current_vote_tally >= 5 AND job_status IN ('created','done') FOR UPDATE SKIP LOCKED (SQL byte-identical to the Task-1 sqlc file); per row the check handler: set job_status running and verify it (non-running -> handler ends without side effects; log Updated Gulag Vote Check Item to Running), fetch the message (context Failed to get Message), DELETE every :gulag reaction off the message (context Failed to delete reaction emoji), then the shared send_to_gulag_and_message path; on success log OK done with sending to gulag and commit: total = current + total, current = 0, voters = [], status done (failed arm: Failed to update message_vote_id {}); on handler failure: log Error running gulag vote + set status failure (its failure arm logs Failed to update JobStatus to Failure for message {}); DB load / connection failures log + skip the iteration (no crash), the loop returns ctx.Err() so errgroup decides (src: gulag/mod.rs:515-569, 591-689)
- [ ] Release loop (RunReleaseCheck, port of run_gulag_check): 1s rhythm; releasable query in_gulag AND release_at <= now() FOR UPDATE SKIP LOCKED (SQL byte-identical to the Task-1 sqlc file); per row: log It's been {} minutes, releasing {} from the gulag; flip in_gulag = false (failure: Failed to update gulag status for user {} + skip row); then remove_from_gulag: fetch member, remove the role, look up the-gulag by name (error arm: the-gulag channel lookup failed; missing: the-gulag channel not found) and post Freeing <@!ID> from the gulag there (Rust remove_from_gulag's success log Removed from gulag; note the release message goes to the-gulag, NOT the stored channel); on success DELETE the row (log Removed from database / Failed to delete gulag user {}) and, when message_id != 0, set that message_votes row to done - the done check log Updated Gulag Vote Check Item to Done / Error updating vote status; on remove error CANONICAL 404 (Unknown Guild / Unknown Message - the core IsDiscordNotFound, errors.As on *discordgo.RESTError with status 404 in the vendored v0.29.0 - NEVER string-matched) DELETEs the stale row (log Removed stale gulag user {} from database / Failed to delete gulag user {} on error recovery); other errors only log (Error run_gulag_check); test-pinned: a synthetic 404 deletes the row, a wrapped 403 / plain error leaves it (src: gulag/mod.rs:404-514)
- [ ] remod no-op note: the remod column exists in the Rust schema (default false) but is only ever SET false and never read - NO behavior to port; every ported SQL statement leaves it untouched (test-pinned against the four loop SQL constants); the Task-1 sqlc projections intentionally omit it so they work against both live and migration-origin DB shapes (src: RUST schema.rs; the sqlc NOTEs in select_gulag_users_releasable.sql.go)
- [ ] /gulag slash (name guLLag, description Send a user to the GuLLag, options user: The user to lookup REQUIRED / reason: Why Are you sending them REQUIRED / length: How Long minutes REQUIRED): silent gate text GuLLag feature is currently disabled (NO period); guild guard no member (NON-ephemeral); invoker member fetch failure Error: Could not verify your permissions; Highly Regarded / admin via the core MemberHasAnyRole -> Error: You need Highly Regarded or admin role to use this command; missing user / reason / length -> the exact Error: Missing required {user,reason,length} option texts (the reason arm compares CommandDataOptionValue::String, i.e. missing-or-non-string collapse to the distinct text); length default 300s, an integer option in (0, 10080] minutes -> length * 60 seconds, <= 0 -> GuLLag length must be positive, else GuLLag length cannot exceed 10080 minutes (1 week); non-user option value -> Please provide a valid user; missing guild arm no member (non-ephemeral); FindGulagRole none -> couldn't find gulag role (NON-ephemeral); canonical core AddToGulag with messageid 0 -> Failed to send to gulag: {} (ephemeral); success (NON-ephemeral): with a string reason Sending <@ID> to the GuLLag for {} minutes, because {} else Sending <@ID> to the GuLLag for {} minutes - {} is the CUMULATIVE fitted length / 60 (not the option, tests pin overflow-extend math) (src: gulag_handler.rs:14-221)
- [ ] /gulag-release slash (description Release a user from the GuLLag, one REQUIRED user option; NOTE: NO feature gate - parity): guild guard via the send_error-shaped Error: This command can only be used in a server (ephemeral); member fetch / role gate texts as /gulag; options-first-missing -> Expected user option; this handler's guild-None arm returns no member Ephemeral (flipped vs /gulag's non-ephemeral - exact match per file); FindGulagRole none -> Error: Couldn't find gulag Role; canonical core IsUserInGulag miss -> Error: Couldn't find user in Database; FindChannel the-gulag: error arm logs Error looking up Gulag Channel: {} then Error: Error looking up Gulag Channel; missing arm -> Error: Couldn't find Gulag Channel; target member fetch failure -> Error: Couldn't get member; GuildMemberRoleRemove failure -> Error: Couldn't remove role; channel send failure -> Error: Couldn't Send message to release; then DELETE the row: success log Removed from database / failure Failed to delete gulag user from DB: {} / pool exhaustion log Failed to get database connection for cleanup: {}; success -> Releasing User from the GuLLag (ephemeral); non-user option value -> Please provide a valid user (non-ephemeral) (src: gulag_remove_handler.rs:14-173)
- [ ] /gulag-list slash (description List users in the Gulag, NO options; NO feature gate): guild-None -> no member (NON-ephemeral); DB failure texts: connection exhaustion Failed to connect to database. Please try again later. vs query failure Failed to query gulag users. Please try again later. (both ephemeral - distinct per file); empty -> No users currently in the Gulag. (ephemeral); rows: per-user fetch failure appends the line \nUnknown user (ID: {}); time info - FUTURE: releases in <std Duration humanized> (test-pinned 300s -> 5m 0s, 3661s -> 1h 1m 1s, sub-second -> 0.5s with trailing-zero trimming); PAST: overdue for release ({}s ago) with raw whole seconds (the Rust test-pinned 41477253s case); header Users in the Gulag: + one \n<@ID> - <time info> per user; response ephemeral (src: gulag_list_handler.rs:14-58 + format_time_info tests 100-154)
- [ ] send_to_gulag_and_message (package-internal, reachable ONLY from the vote path - the /gulag command goes straight to the core AddToGulag with the user-supplied length): FindGulagRole none -> error context Couldn't find gulag role; fixed length 300s; FindChannel the-gulag - BOTH the lookup-error arm (context the-gulag channel lookup failed) and the missing arm (context the-gulag channel not found) are errors (Rust's with_context on Option::None); canonical core AddToGulag (context Failed to add user to gulag); fresh get_message + get_member (raw error propagation); content: Sending <@user> to the Gulag for 5 minutes because of <msg link>, they have {} minutes remaining{user_string} where the {} is the cumulative length / 60 (integer division) and user_string = "\nThese people voted them in" + ", <@ID>" per voted user only when the list is Some (the vote loop passes None - but the arm is test-pinned); posted to the the-gulag channel (src: mod.rs:185-249, 611-646)
- [ ] SetupCommand (d): registers EXACTLY the 4 shapes onto EVERY configured guild from the servers table (mirrors ready(): for server in servers { guild_id.set_commands } - NOT a hardcoded guild list): the 3 slash shapes + Add Gulag Vote kind=Message with byte-identical option strings (pinned in the setup-shape test), NO gulag-vote (dead) registration; per-guild create errors are collected and returned (log-and-continue shape), one missing guild does not skip the others
- [ ] 404 live tests (No remote handler, mock REST verified all ported - see the 404 note above); manual pagination 150-voter parity test (see the reaction note above); command shape byte-identity test; no re-declaration of any test-facing core helper (confirmed: the new files only import and call AddToGulag / IsUserInGulag / FindChannel / FindGulagRole / MemberHasAnyRole / IsTugbot / FormatDuration / DiscordID - none re-declared)

## cull
- [ ] wiring (verified in mod.rs): registered per-guild 9th in `ready()` (src: mod.rs:295); dispatched on command name "cull" (src: mod.rs:227); EVERY cull response sets `defer_response: Some(true)` → the defer response path is the contract (Rust `interaction_create`, serenity `Defer` reply, src: mod.rs:233-241)
- [ ] slash registration shape: name `cull`, description "Cull inactive members from the server", four OPTIONAL options: integer `days` "Inactivity threshold in days (default: 30)", boolean `dry-run` "Preview candidates without kicking (default: false)", boolean `include-never-posted` "Include users who have never posted (default: false)", boolean `scan` "Seed activity data from message history (one-time setup)" (src: cull.rs:27-58)
- [ ] gate order a→m, every gate/DB-error response ephemeral + defer: (a) PROPAGATING `CheckEnabled` on DB key `cull` — disabled → "Cull feature is currently disabled"; DB error → "Failed to check cull feature: {e}"; (b) no guild → "This command can only be used in a guild"; (c) invoker member fetch failure → "Error: Could not verify your permissions"; no whitelist role (Highly Regarded / admin) → "Error: You need Highly Regarded or admin role to use this command"; (d) bot user / bot member / guild fetch failures ("Failed to get bot info: {e}" / "Failed to get bot member: {e}" / "Failed to get guild info: {e}"), bot member lacking KICK_MEMBERS → "I don't have KICK_MEMBERS permission on this server." (src: cull.rs:60-198)
- [ ] option parsing: `days` default 30, validated `1..=365` (out-of-range → "Days must be between 1 and 365"); `dry-run` default false; `include-never-posted` default false; `scan` default false (src: cull.rs:199-241)
- [ ] member list via REST pagination (page size 1000, `after` cursor = last member id, loop until a page returns < 100); fetch failure → posts "Error fetching members: {e}" to cat-herding AND returns ephemeral defer "Failed to fetch members: {e}" (src: cull.rs:242-268)
- [ ] whitelist roles resolved BY NAME ("Highly Regarded", "admin" — the WHITELIST_ROLES constant) with FAIL-CLOSED error ("Failed to resolve whitelist roles: {e}"); bots and whitelist-role members are filtered out (src: cull.rs:270-287, 594-610)
- [ ] already-gulaged users filtered from candidates via `is_user_in_gulag` (only `None` counts) (src: cull.rs:289-293)
- [ ] inactive DB query: `last_message_at < now − days·86400 s` (the Task-1 `select_inactive_user_ids`); query error → cat-herding post "Error querying inactive users: {e}" + abort response (src: cull.rs:307-320)
- [ ] `include-never-posted` ON appends never-posted users (tracked-ids query for the guild); its query failure is only posted to cat-herding ("Failed to query tracked users for never-posted check"), NOT aborted (src: cull.rs:323-339)
- [ ] candidate pipeline: sort by user ID (determinism), dedupe, truncate at MAX_KICKS — Rust does `candidates.sort(); candidates.dedup(); candidates.truncate(MAX_KICKS)` (test-pinned on a >50-entry set with duplicates) (src: cull.rs:341-343)
- [ ] empty candidates: posts "No candidates found (inactive {d}+ days, never posted: {yes|no})" to cat-herding, replies "No candidates found." — the ONE non-ephemeral cull response (ephemeral: false, defer) per the Rust literal (src: cull.rs:344-355)
- [ ] dry-run: batched ONE-roundtrip timestamps query for all candidates (`user_id = ANY`); its failure → cat-herding post + abort; display capped at 25 candidates with "\nand {extra} more..."; block format "**Cull Dry-Run** (inactive {d}+ days, never posted: {yes|no})\n\n{block}\n\nTotal candidates: {n} (capped at 50)\nRun `/cull --days {d}` to execute."; successful post → "Dry-run posted to <#1224402885786472659>"; failed post → inline "Failed to post to <#…>. Dry-run results:\n\n{message}" (ephemeral + defer) (src: cull.rs:357-412)
- [ ] execute mode is NON-BLOCKING by design: max window = MAX_KICKS(50) × KICK_DELAY_MS(1500) = 75 s > the 3 s Discord response window — pinned as a UNIT TEST, not a runtime guard (there is no 24 h check in Rust to port) (test-pinned: 50×1500 ms = 75 000 ms > 3 000 ms) (src: cull.rs:413-416 comment + test `test_execute_mode_max_kicks_would_block`)
- [ ] execute mode: kick loop spawned as a background task (Rust `tokio::spawn` → Go goroutine) BEFORE the reply: immediate ephemeral defer "Cull started: {N} candidates. Results will be posted to <#1224402885786472659>"; the loop posts "Starting cull: {N} candidates (inactive {d}+ days)...", then per candidate `kick member` with reason "Inactive {d} days — /cull by {username}" (kick errors are counted as skips + logged `[cull] Failed to kick {uid}: {e}`), sleeping KICK_DELAY_MS between every kick, then final "Cull complete: {k} kicked, {s} skipped (errors)." (src: cull.rs:413-455)
- [ ] `scan` = one-time seed of `user_activity`, ALSO background-spawned (immediate ephemeral defer "Scan started. Results will be posted to <#1224402885786472659>."), with the 180-day cutoff computed from the CONSTANT `180 * 86400` seconds minus from now — the adjacent source comment says "(90 days)" which is STALE and WRONG; the constant (6 months) is what is ported, not the comment; per text channel only (kind == Text), pages of 100 messages newest-first (before-cursor = oldest message of the page), 200 ms inter-page delay, stop conditions checked AFTER processing the page (a page whose oldest crosses the cutoff still has its valid messages recorded): short page (< 100) stops, oldest ts < cutoff (strict less-than: a message exactly 180 days old is still recorded) stops; webhooks and bot authors are excluded (test-pinned boundary: 179d/180d/181d old relative to the cutoff); per channel the message counter counts ALL messages in the page (bots/webhooks included in the count); dedupe of (user, guild) pairs; then `upsert_user_activity` applied per pair with a shared `now` and the `GREATEST(existing, excluded)` anti-regression update (sqlc v1 can't resolve array params in `UNNEST`, so the Go port uses the committed Task-1 single-pair query in a per-pair loop — the committed .sql header documents this loop shape; the GREATEST anti-regression is byte-identical) (src: cull.rs:471-632)
- [ ] scan completion posts to cat-herding with counts: "Scan complete: {u} unique users tracked (scanned {m} messages across {c} channels). Initiated by {user}" / the no-users variant / failure variants ("Scan failed: could not fetch channels: {e}" / "Scan failed to save results: {e}") are logged + posted (src: cull.rs:528-631)
- [ ] pinned constants (unit test table): MAX_KICKS = 50; KICK_DELAY_MS = 1500; CAT_HERDING_CHANNEL_ID = 1224402885786472659; WHITELIST_ROLES = ["Highly Regarded", "admin"]; day-seconds constant = 86400 (src: cull.rs:21-26)
- [ ] `format_timestamp` → Go `time.Time.Format("2006-01-02")` (the Rust Hinnant days-since-epoch arithmetic is NOT ported — the std lib is provably equivalent); pre-epoch input → "unknown"; equivalence table pinned (1970-01-01 / 2000-01-01 / 2024-01-15 / 2024-02-29 / 2025-06-15) (src: cull.rs:638-666 + tests 672-700)
- [ ] fresh-DB flag parity: the `cull` features row exists only in the live production DB / migration 2026-06-25-000001; on a fresh DB the gate is the PROPAGATING CheckEnabled (missing row → Ok(false)) → "Cull feature is currently disabled" in BOTH languages
